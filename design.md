# Aaalice NAI Launcher 设计规范

> **设计方向：Quiet Layered Utility / 静谧层叠工具界面**
>
> 适用于 Windows/macOS 桌面端与 Android 手机、横屏、平板和大屏。共享功能必须从设计开始同时考虑指针、键盘和触屏，不能先完成桌面版，再把移动端做成缩小后的补丁。

## 1. 设计目标

Aaalice NAI Launcher 是高频创作工具。提示词、图像、参数和操作是视觉主体，容器和装饰退到背景。

- **安静**：默认状态低对比，重点只在需要时出现。
- **清晰**：优先用排版、留白和色面建立层级。
- **内容优先**：装饰不得挤压创作内容或降低可读性。
- **跨端一致**：任务、术语、状态和结果一致；导航与面板可随设备变化。
- **各端高效**：桌面保留并行信息和键鼠效率；移动端保证触达、命中和连续操作。
- **主题统一**：主题可改变气质，不能改变信息架构、控件语义和可用性。

一句话判断：**先拿掉边框；层级仍然清楚时，就不要把边框加回来。**

## 2. 基础设计系统

项目基于 Flutter Material 3：

- `lib/presentation/themes/core/theme_composer.dart` 组合颜色、字体、形状和动效。
- `lib/presentation/themes/theme_extension.dart` 提供已接入的圆角、动效和分隔语义。
- `lib/presentation/themes/design_tokens.dart` 提供稳定的通用尺寸。
- `lib/presentation/themes/core/input_surface_style.dart` 与 `lib/presentation/widgets/common/input_surface_container.dart` 统一输入表面。
- `lib/presentation/widgets/common/` 承载跨页面公共组件。

样式来源优先级：

1. 公共组件表达的语义角色和状态。
2. `Theme.of(context).colorScheme` / `textTheme`。
3. `AppThemeExtension`。
4. `DesignTokens`。
5. 有明确业务理由的局部例外。

禁止先写局部颜色、圆角、间距和时长，再要求主题适配。新增 token 必须完成主题挂载和实际消费，不能只声明不使用。

## 3. 视觉层级

### 3.1 内容先于容器

- 先用标题、间距和对齐分组，再考虑色面，最后才考虑边框。
- 只有独立滚动、独立交互或明确语义边界才需要容器。
- 普通页面不要堆叠“大圆角卡片 + 图标底座 + 标题 + 副标题”。
- 嵌套容器通常不超过两层；第三层必须有明确交互理由。
- 只读元数据优先用文字或“图标 + 文字”，不要伪装成控件。

### 3.2 四级表面

| 层级 | 用途 | 推荐来源 | 默认效果 |
|---|---|---|---|
| Canvas | 页面背景、主工作区 | `surface` | 无边框、无阴影 |
| Section | 侧栏、分组、静态卡片 | `surfaceContainerLow` | 默认无边框 |
| Control | 输入、hover、交互行 | `surfaceContainer` / `surfaceContainerHighest` | 填充色面 |
| Overlay | 菜单、浮层、对话框 | `surfaceContainerHigh` | 柔和阴影，必要时细边界 |

- 同一页面最多同时出现三个明显表面层级。
- 图片、提示词和主操作必须高于容器装饰。
- 暗色界面不用白色描边模拟层级。

### 3.3 语义颜色

- `primary`：主操作、当前选择、焦点和关键进度。
- `secondary` / `tertiary`：来源和次级分类，不能与主操作竞争。
- `error`：失败、危险操作和不可恢复结果。
- `onSurfaceVariant`：辅助说明和次要元数据。
- success、warning、info、队列状态和来源类型使用具名业务语义，不在页面散落固定色。
- 禁用态从当前前景色降低透明度，不创建另一套灰色常量。

建议范围：hover 4%–9%，selected 10%–14%，disabled 前景约 38%，subtle divider 12%–24%。常用值应进入主题或 token，不成为页面 magic number。

## 4. 边框、阴影与材质

### 4.1 默认规则

- 普通卡片、工具按钮、导航项、chip 和已填充控件默认无完整描边。
- 只有色面不足以解释结构时，才使用低对比 1 logical pixel 分隔线。
- 不允许“外框 + 内框 + 分隔线”重复表达同一分组。
- 不允许纯白或高不透明度浅色线框包围普通暗色元素。

### 4.2 可以使用边界的场景

- 拖放目标、框选、裁剪等需要精确边界的交互。
- 图片与背景接近时的 1px 内侧保护线。
- 表格、时间轴和复杂数据中的结构线。
- 同色浮层无法通过阴影区分时的低对比边界。
- 键盘焦点和错误状态，但不能改变控件几何尺寸。

### 4.3 阴影与材质

- 静态内容和输入表面默认无阴影。
- 菜单、浮动工具条和 tooltip 使用一级柔和阴影。
- 对话框使用二级阴影和 scrim，不靠重阴影强行分层。
- hover 卡片最多使用轻微 elevation，不能同时出现亮边框。
- 纹理只能作为低对比背景，不覆盖文字、输入区和图像细节。
- 玻璃效果只用于确有背景穿透价值的浮层。
- 普通控件不得叠加“渐变 + 描边 + 发光 + 阴影”。

## 5. 排版、间距与形状

### 5.1 排版

| 角色 | Theme 样式 | 常用尺寸 | 字重 |
|---|---|---:|---:|
| Page title | `titleLarge` / `headlineSmall` | 20–24 | 600 |
| Section title | `titleMedium` | 16 | 600 |
| Item title | `titleSmall` / `bodyMedium` | 14 | 500–600 |
| Body | `bodyMedium` | 14 | 400 |
| Supporting | `bodySmall` | 12 | 400 |
| Control label | `labelLarge` / `labelMedium` | 12–14 | 500 |

- 一个局部区域最多使用三个明显字号层级。
- 标题依靠字号、字重和间距，不依赖描边或高饱和色。
- 中文正文不额外增加 letter spacing。
- 路径、模型、种子和成列数字使用便于扫描的字形；数字启用 tabular figures。
- placeholder 不能替代永久标签。

### 5.2 间距

项目使用 4px 基础网格：4、8、12、16、24、32。

- 桌面页面水平边距通常 20–24px；紧凑屏幕 12–16px。
- 同组元素间距必须小于组间距。
- 图标与文字通常间隔 8px。
- 表单/设置内容最大宽度建议 840–960px；画廊和画布不强制限宽。
- 标准控件高 40–44px；桌面紧凑工具按钮可为 32–36px，但命中区不能同步缩小。
- 使用基线和视觉中心对齐，不按图标边界框机械对齐。

### 5.3 形状

业务组件声明语义角色，不自行决定数值：

- `micro`：微型装饰和角标。
- `control`：按钮、输入和导航选中面。
- `card`：分组和普通卡片。
- `dialog`：对话框和大型浮层。
- `circle`：头像、状态点和圆形图标控件。
- `pill`：chip、segmented control 和状态筛选，不是所有按钮的默认形状。

父子圆角应递减；内层只有在 chip 或圆形控件时才可比外层更圆。

## 6. 组件规范

### 6.1 导航与页面

- 页面头部通常 52–56px，只放标题和最高频操作。
- 全局导航稳定，页面内部导航更轻，不重复全局标题和说明。
- 当前导航项使用淡主色背景和主色前景，默认无描边。
- 桌面侧栏与内容区用 surface 区分；移动端可改用抽屉、底部导航或紧凑 rail。

### 6.2 卡片与分组

- 默认分组方式：标题 + 留白；其次是低对比色面；最后才是边框。
- 静态卡片使用 `surfaceContainerLow`，无边框、无阴影。
- hover 提高色面对比；selected 使用主色淡底。
- 卡片小节用 16–24px 留白或局部短分隔线，不再嵌套卡片。
- 图像内容卡片按第 8.3 节处理，不套用普通静态卡片反馈。

### 6.3 按钮

优先级：`FilledButton` → `.tonal` → `TextButton` → `IconButton` → 必要时 `OutlinedButton`。

- 一个操作区域通常只有一个主操作。
- 普通图标按钮无常驻边框，hover/press 使用轻色面。
- 相邻按钮不能全部高强调。
- 危险操作在最终确认前使用 error 前景或淡 error 色面。
- loading 替换内容时保持按钮宽高不变。
- 仅图标按钮必须有 tooltip 和语义标签。

### 6.4 输入框

- 搜索、单/多行文本和提示词编辑器共享统一的深色填充 Control surface。
- 默认无边框、无外投影、无纹理和第二层外框。
- hover 可轻微提高背景对比；focus/error 只在控件内侧显示柔和高亮。
- 所有状态的 border dimensions 恒为 `0`，不得改变宽高、padding 或相邻布局。
- disabled/read-only 通过内容和填充透明度降级；无需编辑能力时直接使用普通文本。
- prefix/suffix 图标保持次级权重，操作按钮拥有足够命中区。
- 多行编辑器优先保障正文面积；工具操作放在框外紧凑 footer，不覆盖正文。

### 6.5 筛选与设置

- Chip 只用于短标签、状态和可移除条件。
- 互斥模式优先使用 segmented control 或 tab。
- 筛选很多时使用横向滚动、折叠或筛选面板，不压缩为难懂纯图标。
- 设置页按用户任务分组，不按 provider/service 分组。
- 常规设置项使用标题、可选说明和右侧控件，不必每行加分隔线。
- Switch 只用于即时布尔状态；昂贵或需确认的操作使用按钮或对话框。

### 6.6 对话框、加载与错误

- 仅必须打断流程的决策使用对话框；能原位完成就不弹窗。
- 标题简短，正文先说结果，再说原因或风险。
- 操作区保持“低强调取消在前，主操作在后”。
- 浮层必须有明确锚点，管理焦点，并支持 Esc/系统返回关闭。
- 首次加载使用骨架或居中进度；局部刷新保留已有内容。
- 错误说明发生了什么和下一步怎么做，不只显示异常字符串。
- toast 只用于短暂确认，不承载必须阅读或操作的信息。

## 7. 跨平台布局

### 7.1 断点

断点由内容可用性决定，不按平台名或设备型号硬编码：

- `< 600px`：紧凑手机，单列主流程，使用抽屉、底部导航、底部面板或全屏次级页面。
- `600–840px`：横屏手机/平板，可使用紧凑双区、NavigationRail 或并排预览。
- `840–1180px`：标准桌面/大屏平板，可使用稳定侧栏和主辅双面板。
- `> 1180px`：宽桌面，可增加内容列或辅助面板，但不盲目拉宽表单。

方向变化后必须保留输入、滚动位置、选择和任务状态。

### 7.2 响应式规则

- 先定义任务优先级，再决定导航、主内容、辅助面板和操作区。
- 不通过等比例缩放、裁切、缩小文字或任意隐藏得到手机版。
- 业务能力、字段含义、状态和操作结果保持跨端一致。
- 桌面侧栏可在手机变成抽屉或底部导航；辅助面板可变成底部面板或独立页面。
- 低频操作可进入语义清楚的 overflow menu，但不能静默删除。
- 手机表单通常单列；只有宽度充足时才并排关联字段。
- 桌面分栏设置合理最小宽度；移动端不依赖精细拖拽分隔条。
- 固定底栏和底部面板必须避开 `SafeArea`、系统手势区和软键盘。
- 使用局部 `LayoutBuilder.constraints` 判断宽度，不用设备型号或猜测的侧栏宽度。
- 不使用 `FittedBox` 缩小含文字或交互控件的工具栏。
- 在线画廊桌面/平板顶栏遵守 `AGENTS.md` 专用约束；紧凑手机保留同一能力和职责边界，改用分层筛选面板。

### 7.3 输入方式

- **桌面**：支持鼠标、触控板、键盘、hover、焦点顺序、快捷键、精确滚动和上下文操作。
- **移动端**：核心任务可通过单击、长按、滑动和系统返回完成，不依赖 hover、右键或外接键盘。
- 同一操作提供等价入口和一致结果，例如桌面右键菜单对应移动端长按菜单。
- 手势必须可发现且有反馈；危险操作不能只靠无提示滑动触发。
- 水平手势不得与系统返回、图片浏览和嵌套滚动争抢。
- 平台差异集中在导航壳层、capabilities/service/conditional import；共享页面不散落 `Platform.isAndroid`，不复制业务流程。

## 8. 动效与反馈

### 8.1 节奏

- hover / press：100–140ms。
- 普通状态：160–200ms。
- 面板展开和页面内切换：200–260ms。
- 对话框或大型布局变化：240–320ms。
- 进入和状态变化使用平滑 ease-out；退出更短。
- 功能界面默认不使用 bounce、elastic、旋转和持续漂浮。

时长与曲线必须来自 `AppThemeExtension` 或公共 token。

### 8.2 行为

- 状态反馈优先改变背景、前景和透明度，不改变文字字重和几何结构。
- hover 只能增强桌面体验，不能承载必要信息或唯一入口。
- 触屏按下反馈即时；滚动中的卡片不得误触点击。
- 展开/折叠同时处理进入和退出；关闭面板后回到原任务位置。
- 超过约 300ms 的异步操作显示局部 loading。
- 后台切换、网络切换和屏幕旋转不能重复提交操作。
- 尊重 `MediaQuery.disableAnimations`，关闭非必要位移和缩放。

### 8.3 图像卡片

生成结果、历史、本地图库和在线画廊统一使用 `ImageCardHoverMotion`：

- 桌面 hover 目标为约 `1.01` 倍缩放，时长和曲线来自主题。
- 缩放只影响绘制，不改变网格占位和相邻布局。
- 可同时使用一级柔和阴影和操作按钮显现，不叠加亮边框或发光。
- selected、focused、loading 和拖拽状态使用独立语义。
- 移动端通过 press/selected 反馈提供等价确认，不依赖 hover。
- `disableAnimations` 时立即回到 `1.0`，保留必要的静态反馈。

## 9. 可访问性与本地化

- 正文与背景至少满足 WCAG AA；关键信息不用低透明度灰字。
- selected、error、success 不能只依赖颜色，同时使用图标、文字或位置。
- 自定义控件提供 `Semantics`、disabled、focus、Enter 和 Space 行为。
- 桌面点击目标通常不小于 40×40px。
- Android 触屏目标优先 48×48 logical pixels，最低不小于 44×44。
- 紧凑图标可以小，但命中区和防误触间距不能同步缩小。
- 移动端处理 `SafeArea`、刘海/挖孔、系统栏、系统返回、横竖屏和软键盘。
- 输入获得焦点后字段保持可见；键盘关闭后布局和滚动位置稳定。
- 文本缩放后，标题和按钮不能被固定高度裁切。
- 中、英、日文案长度不同；布局允许弹性、截断或合理换行。
- 不用字符串空格和硬编码坐标模拟布局。

## 10. 多主题边界

所有主题必须保持：

- 信息架构、控件语义和操作顺序。
- 间距节奏、密度范围和对齐原则。
- 无边框优先的层级策略。
- default / hover / pressed / selected / focused / disabled / loading 状态。
- 可读性、命中区、键盘焦点和响应式行为。
- 动效目的和大致时长。

主题可以改变 ColorScheme、字体家族、约定范围内的圆角、轻量环境阴影和不覆盖内容的背景装饰。

主题不得恢复高对比描边、全胶囊按钮、全大圆角卡片、弹跳动效、内容纹理或影响布局稳定性的尺寸变化。公共组件还要保持亮暗配色正确，即使当前入口固定为暗色模式。

## 11. Flutter 实现约定

- 优先复用 Material 3 控件和 `lib/presentation/widgets/common/`。
- Button 使用 `FilledButton` / `.tonal` / `TextButton` / `IconButton`；现有 `ThemedButton` 只作兼容入口。
- Text input 使用 `ThemedInput`、`ThemedFormInput` 或统一 `InputDecorationTheme`。
- Checkbox、Switch、Slider、Radio 使用原生 Material 控件或复用原生行为的 themed 入口。
- 普通 Card/Panel 使用简单 surface 容器；`ElevatedCard` 只用于确有 elevation 的交互层。
- Dialog 复用现有 themed shell；确认操作使用 `ThemedConfirmDialog`。
- Toast 使用 `AppToast`，Tooltip 使用主题 `Tooltip` / `ShortcutTooltip`。
- Divider 只表达真实结构，颜色从主题获取。
- 公共组件 API 表达 `selected`、`danger`、`emphasis` 等语义，不暴露大量颜色参数。
- 共享业务组件、Provider、路由状态和操作命令跨端复用。

## 12. UI 验收清单

### 视觉

- [ ] 第一眼能识别主内容和主操作。
- [ ] 没有白色线框、嵌套卡片或重复装饰堆叠。
- [ ] 同类控件的高度、圆角、图标和间距一致。
- [ ] 页面最多三个明显表面层级。
- [ ] 长文案、主题变化和文本缩放下仍可读。

### 交互

- [ ] hover、pressed、selected、focused、disabled、loading 状态完整且不引起布局跳动。
- [ ] 桌面可用鼠标、触控板和键盘完成高频流程。
- [ ] 移动端不依赖 hover、右键或外接键盘。
- [ ] 单击、长按、滑动、Esc、Enter、Tab、方向键和系统返回符合控件类型。
- [ ] 设备旋转和软键盘开合不丢状态、不重复提交。
- [ ] loading 和错误原位呈现，不让旧内容无故消失。

### 响应式

- [ ] 与改动相关的页面覆盖 360、412、600、840、1180、1600px 中必要宽度。
- [ ] 移动端覆盖竖屏及至少一个横屏/平板场景。
- [ ] 在线画廊桌面布局额外覆盖 700、840、1180、1600px。
- [ ] 同一功能在手机、平板和桌面均可完成，没有静默缺失。
- [ ] 已检查 `SafeArea`、系统手势区、底部面板、软键盘、系统返回、窗口缩放和分栏最小宽度。
- [ ] 无 `RenderFlex overflow`、文字裁切、控件遮挡和过小命中区。

### 工程与运行

- [ ] 复用现有主题、公共组件、Provider 和操作命令。
- [ ] 没有新增无理由的硬编码颜色、圆角、间距和 Duration。
- [ ] UI 改动有针对性 widget test 或最小回归验证，覆盖关键断点。
- [ ] 共享 UI 已分别在 Windows Debug runner 与 Android emulator/设备完成真实交互和本轮新截图检查。
- [ ] 一端通过没有被用来推断另一端通过。

## 13. 最终取舍

遇到未覆盖的问题时，依次判断：

1. 是否让用户更快理解并完成任务？
2. 是否让主要内容比容器更醒目？
3. 是否能用留白或色面代替边框？
4. 是否复用了已有主题语义和组件？
5. 是否在桌面、移动端、不同宽度、方向、输入方式、主题和语言下成立？
6. 是否保持跨端业务一致，同时符合各端操作习惯？
7. 是否减少视觉噪声和未来维护成本？

功能相同时，选择**容器更少、边框更少、状态更清楚、跨端更自然、动效更克制**的方案。
