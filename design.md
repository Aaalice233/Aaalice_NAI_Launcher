# Aaalice NAI Launcher 设计语言基准

> **设计方向：Quiet Layered Utility / 静谧层叠工具界面**
> 本文是项目 UI 的统一设计基准，适用于新界面、现有界面重构、公共组件、主题预设和代码审查。

## 1. 设计目标

Aaalice NAI Launcher 是面向高频创作流程的桌面工具，不是营销网页，也不是由大量独立卡片拼成的仪表盘。界面应当让提示词、图像、参数和操作成为视觉主体，让容器退到背景中。

最终观感应当是：

- **安静**：默认状态低对比，重点只在需要时出现。
- **清晰**：层级主要依靠留白、色面和排版，而不是白色线框。
- **紧凑但不拥挤**：适合桌面高频操作，同时保留舒适的点击与阅读空间。
- **统一但不单调**：不同主题可以改变色彩、字体和少量装饰气质，但不能破坏共同的布局、层级和交互语法。
- **内容优先**：创作内容永远比容器、装饰和品牌效果更醒目。

一句话判断标准：**先拿掉边框；如果层级仍然清楚，就不应把边框加回来。**

---

## 2. 当前项目的设计基础

### 2.1 已有设计语言

项目当前建立在 Flutter Material 3 之上，并已有模块化主题系统：

- `lib/presentation/themes/core/theme_composer.dart` 负责组合颜色、字体、形状与动效，并生成统一的 Material 3 组件主题。
- `lib/presentation/themes/theme_extension.dart` 只保留已接入消费链路的圆角、动效和低对比分隔语义。
- `lib/presentation/themes/design_tokens.dart` 提供部分跨界面 token。
- `lib/presentation/widgets/common/` 和设置页公共组件承载了部分统一实现。
- 生成页采用工作区式布局；在线画廊强调内容密度；设置页采用侧栏与受限宽度内容；统计页采用响应式信息网格。

当前 `MaterialApp.router` 固定使用 `ThemeMode.dark`；未保存设置时默认外观是 `grungeCollage`，默认字体配置是 `LXGW ZhenKai GB`。每个预设都通过 `ThemeComposer` 生成完整 `ThemeData`，并全局挂载 `AppThemeExtension`；公共卡片、输入、按钮、选择控件与页面转场从这些真实 token 获取样式。旧 shadow / effect / texture / divider 模块及未接线 painter 已移除，避免主题声明一套、运行界面又实现另一套。`DesignTokens` 仍承担稳定的通用尺寸，页面不得把它与局部常量混成新的并行系统。

这些实现已经形成几项适合保留的项目特征：

1. **桌面工具属性明确**：侧栏导航、工具栏、可滚动工作区和快捷操作是主体。
2. **Material 语义完整**：颜色与文本大多能从 `ThemeData` / `ColorScheme` 派生。
3. **主题具有个性**：项目允许 Grunge、Zen、Material、Flat 等多种视觉风格。
4. **交互反馈较丰富**：已有 hover、pressed、selected、loading 和自适应布局实现。
5. **内容区类型清楚**：编辑器、图像网格、设置表单和统计信息拥有不同密度需求。

### 2.2 代表性界面的风格判断

- **全局导航**：`main_nav_rail.dart` 采用紧凑侧栏、图标与标签组合，通过淡色 hover、选中色面和前景色反馈状态，不为 selected 状态重复叠加完整描边。
- **生成页**：`generation/` 是项目核心视觉原型，采用画布/预览与参数面板组成的工作台式布局，强调连续编辑和空间利用；它应成为“内容优先、工具退后”的主要参考，而不是继续增加独立卡片。
- **在线画廊**：`online_gallery_screen.dart` 使用高密度顶栏与无界图片网格，内容优先最明确。这里的筛选与批量操作需要紧凑色面状态，不适合大量 outlined controls。
- **设置页**：`settings_screen.dart` 使用侧栏、受限内容宽度和任务分区；`SettingsCard` 以留白和低对比色面分组，不再用常驻外框与底部分隔线重复包围内容。
- **统计页**：`statistics_screen.dart` 使用响应式瀑布流卡片，适合浏览不同数据模块；卡片层级需要统一，图表本身应高于容器装饰。
- **公共输入与容器**：`ThemedInput`、`ElevatedCard`、`CompactIconButton` 等已经尝试统一交互，但边框、内凹阴影、悬浮阴影等表达同时存在，导致调用方难以组合出安静的界面。
- **对话框与浮层**：项目同时存在 themed、glass 与功能专用实现。它们应共享遮罩、圆角、操作区、键盘焦点和层级规则，仅在内容材质上保留有限差异。

总体上，项目最适合的不是纯 Material 默认风格、重拟物风格或卡片化后台，而是：**以生成工作台和图片内容为中心，以 Material 3 语义为基础，用低对比色面建立深度的桌面创作工具风格。**

### 2.3 本轮统一所针对的问题

本轮全仓审计发现的问题不是“缺少样式”，而是**局部样式过多且没有稳定优先级**：

- 卡片、按钮、输入框和选中项经常各自添加完整描边，形成层层白框。
- `Border.all`、`OutlinedButton`、`OutlineInputBorder` 与自定义阴影并存，同一层级被重复表达。
- 圆角、间距、持续时间和透明度存在大量局部常量，公共 token 没有成为默认入口。
- 部分公共组件本身带固定边框，调用方再套容器后容易产生嵌套框。
- 旧主题预设曾同时声明颜色、形状、阴影、纹理和动效，其中部分能力从未接入运行界面，个性声明强于产品一致性。
- 某些界面依靠重阴影、弹跳曲线或高对比描边表达普通状态，抢走了内容注意力。

因此，本基准不是再增加一层装饰，而是规定视觉信息的优先级：

> **排版与留白 → 色面 → 状态色 → 阴影 → 细分隔线 → 完整描边。**
> 越靠后的手段，使用频率越低。

---

## 3. 核心原则

### 3.1 内容先于容器

- 不要为了“规整”给每组内容都套一张卡片。
- 先用标题、间距和对齐分组；只有需要独立滚动、独立交互或明显语义边界时才使用容器。
- 图片、提示词和主要参数应拥有最高视觉权重。
- 装饰不能降低可读性、缩小主要内容或制造无意义层级。

### 3.2 无边框优先，细边框兜底

- 默认容器、工具按钮、导航项、普通卡片使用**无边框**样式。
- 相邻区域优先使用不同层级的 `surface` 色面区分。
- 只有色面不足以解释边界时，才使用低对比 1px 分隔线。
- 不允许用纯白或高不透明度浅色线框包围暗色界面中的普通元素。
- 不允许“外框 + 内框 + 分隔线”同时表达同一个分组。

### 3.3 一次只强调一件事

- 一个操作区域通常只有一个主操作。
- 选中状态使用“淡色底 + 强色图标/文字”，不再叠加粗边框、阴影和缩放。
- 警告色只用于需要用户处理的风险，不作为普通装饰色。
- 大面积主题纹理只用于背景或空白装饰区，不覆盖高密度表单与数据内容。

### 3.4 状态变化不应改变几何结构

- hover、selected、loading 不应导致文字变粗后跳动、控件尺寸变化或列表重排。
- 交互反馈优先改变背景色、前景色和透明度。
- 边框出现时必须预留空间，避免 focus 后布局抖动。
- 异步加载应保留原容器尺寸，避免页面上下跳动。

### 3.5 功能界面保持克制

- 功能性控件不使用弹簧、弹跳、旋转或持续漂浮来吸引注意。
- 不把每个区块做成“大圆角卡片 + 图标圆底 + 标题 + 副标题”的重复模板。
- 不使用无意义渐变、玻璃拟态或发光效果。
- 个性主题可以有气质，但不能让控件难以识别或降低操作效率。

---

## 4. 视觉层级与色面

### 4.1 四级表面

所有界面优先从 `ColorScheme` 派生，不直接写死黑白色。

| 层级 | 用途 | 推荐来源 | 边框/阴影 |
|---|---|---|---|
| Canvas | 页面背景、主工作区 | `surface` | 无 |
| Section | 侧栏、分组色块、静态卡片 | `surfaceContainerLow` | 默认无 |
| Control | 输入框、hover 项、可交互行 | `surfaceContainer` / `surfaceContainerHighest` | 默认无 |
| Overlay | 菜单、浮层、对话框 | `surfaceContainerHigh` | 轻阴影，必要时极细边界 |

使用规则：

- 同一页面最多让三个表面层级同时可见。
- 相邻容器不要只相差几乎不可见的颜色后再补一圈亮边；应先调整色面关系。
- 静态卡片通常使用 Section 层，不加阴影。
- 菜单、tooltip、下拉面板和对话框才使用 Overlay 层与阴影。
- 暗色主题中不能用白色描边模拟层级；使用略亮的表面色和柔和环境阴影。

### 4.2 语义颜色

- `primary`：主操作、当前选择、焦点和关键进度。
- `secondary` / `tertiary`：来源标识、次级分类或主题个性；不能与主操作竞争。
- `error`：失败、危险操作和不可恢复结果。
- `onSurfaceVariant`：辅助说明、未选图标、次要元数据。
- 禁用态从当前前景色降低透明度，不创建新的灰色常量。
- success、warning、info、队列状态、来源类型、性别标识和图片遮罩必须逐步收敛为具名业务语义色；页面不得继续散落 `Colors.green`、固定性别色或来源色。

推荐状态透明度起点，可按亮暗主题微调：

| 状态 | 建议处理 |
|---|---|
| Hover | `onSurface` 4%–7% 色面，或 `primary` 6%–9% |
| Selected | `primary` 10%–14% 色面 + `primary` 前景 |
| Pressed | 在 hover/selected 基础上再增加约 3%–5% |
| Disabled | 前景 38%，交互色面 4% 以下 |
| Subtle divider | `outlineVariant` 12%–24% |
| Focus | 1px `primary` 焦点环或等价的高可见焦点色面 |

透明度是层级范围，不是要求每个组件写局部 magic number；常用状态应收敛到主题扩展或公共 token。

---

## 5. 边框与分隔线基准

### 5.1 可以使用边框的场景

- 文本输入获得键盘焦点时的焦点环。
- 拖放目标、框选区域或裁剪区域等需要精确边界的交互。
- 图片与背景颜色过于接近时的 1px 内侧保护线。
- 表格列、时间轴或复杂数据图中确实承担结构含义的线。
- 同色浮层与背景无法通过阴影稳定区分时的低对比边界。

### 5.2 不应使用边框的场景

- 普通设置卡片、静态信息块和页面分组。
- 工具栏图标按钮、导航项和只读 badge。
- 已经有明显填充色的按钮、选中项和输入框。
- 外层已有容器，内层只是内容分组时。
- 仅为了让界面“看起来完整”时。

### 5.3 细边框参数

- 常规宽度：`1` logical pixel；不要使用 2px 装饰边框。
- 默认颜色：`outlineVariant` 的 12%–24%，而不是 `Colors.white`。
- 图片保护线：根据图片背景使用黑或白 8%–12% 的内侧线。
- 焦点边界允许提高对比，但不能再叠加外发光。
- 分隔线只跨越需要分隔的内容宽度，不必机械地铺满整个容器。

---

## 6. 排版

字体家族可以随主题变化，但信息层级必须稳定。

| 角色 | Theme 样式 | 常用尺寸 | 字重 | 用途 |
|---|---|---:|---:|---|
| Page title | `titleLarge` / `headlineSmall` | 20–24 | 600 | 页面主标题 |
| Section title | `titleMedium` | 16 | 600 | 一级分组 |
| Item title | `titleSmall` / `bodyMedium` | 14 | 500–600 | 行标题、卡片标题 |
| Body | `bodyMedium` | 14 | 400 | 正文与主要说明 |
| Supporting | `bodySmall` | 12 | 400 | 辅助说明、元数据 |
| Control label | `labelLarge` / `labelMedium` | 12–14 | 500 | 按钮、标签、筛选项 |

规则：

- 一个局部区域最多使用三个明显字号层级。
- 标题通过字号、字重和间距建立层级，不依赖全大写、描边或高饱和色。
- 常规正文避免 `FontWeight.w700`；600 已足够表达强调。
- 提示词、路径、模型名、种子和数值等技术内容使用适合扫描的字形；成列数值启用 tabular figures。
- 中文正文不额外增加 letter spacing；英文标签也只使用主题定义的轻微字距。
- 文本颜色只分主要、次要、禁用和语义状态，避免每个区域发明一种灰色。
- placeholder 不能替代永久标签；字段含义在输入后仍需可知。

---

## 7. 间距、密度与对齐

项目采用 4px 基础网格：

| Token | 值 | 常见用途 |
|---|---:|---|
| `xxs` | 4 | 图标内部、紧邻元数据 |
| `xs` | 8 | 图标与文字、紧凑控件间距 |
| `sm` | 12 | 行内控件、卡片内部小间距 |
| `md` | 16 | 标准内边距、分组间距 |
| `lg` | 24 | 页面边距、一级区块间距 |
| `xl` | 32 | 大区块分隔、空状态留白 |

规则：

- 常规桌面页面水平边距 20–24px；窄屏 12–16px。
- 表单/设置内容建议最大宽度 840–960px；画廊、编辑器等内容型页面不强制限宽。
- 标准控件高度 40–44px；紧凑工具栏按钮可为 32–36px，但必须有 tooltip 和足够点击区域。
- 导航行保持约 44–48px 高，确保快速命中。
- 同组元素间距必须小于组与组之间的间距。
- 图标与相邻文字优先 8px；不要在视觉关联很强的元素之间使用 16px 以上空隙。
- 使用基线和视觉中心对齐，不以图标边界框机械对齐。
- 只读元数据优先做成文字或“图标 + 文字”，不要套成大型控件。

---

## 8. 形状与圆角

业务组件只声明 `micro`、`control`、`card`、`dialog`、`pill` 等语义角色，不自行决定普通圆角数值。当前 token 尚未完全收敛时，以下数值作为默认深色主题的参考实现，而不是页面硬编码许可：

- `micro`：4px，用于微型装饰和紧凑角标。
- `control`：8px，用于按钮、输入框和导航选中面。
- `card`：12px，用于分组色块和普通卡片。
- `dialog`：16px，用于对话框和大型浮层。
- `circle`：头像、状态点、仅图标的圆形语义控件。
- `pill`：明确的 chip、segmented control、状态筛选；不能成为所有按钮的默认形状。

每套主题可以为语义 shape token 提供有限变化，但必须满足：

- 父子容器的圆角有明确递减关系。
- 内层不能比外层更圆，除非内层是 chip 或圆形按钮。
- 同一行同类控件使用相同圆角。
- 不用大圆角掩饰分组过多；如果页面到处是独立圆角块，应先减少容器数量。

---

## 9. 阴影、材质与纹理

### 9.1 阴影

- 静态内容默认无阴影。
- hover 卡片最多使用非常轻的 elevation 变化，并且不能同时出现亮边框。
- 下拉菜单、浮动工具条和 tooltip 使用一级柔和阴影。
- 对话框使用二级阴影；背景同时使用 scrim，不靠更重阴影硬拉开层级。
- 硬偏移阴影只允许用于主题预览、品牌插画等装饰区域，不用于高频表单和工具按钮。

### 9.2 纹理与玻璃效果

- 纹理只能作为低对比背景气质，不能覆盖文字、输入区域和图像细节。
- 玻璃效果只在确有背景穿透价值的浮层中使用；普通面板不使用模糊。
- 不允许“渐变 + 描边 + 发光 + 阴影”叠加在一个普通控件上。
- 任何主题效果关闭后，信息层级仍必须成立。

---

## 10. 图标与图像

- 默认使用同一套 Material Symbols / Material Icons 视觉体系，不混用 emoji 充当功能图标。
- 工具栏图标通常 18–20px；导航和页面标题图标 20–24px。
- 同一行图标保持一致的 optical size 与视觉重量。
- 有 filled / outlined 两种图标时，可用 filled 表达 selected，outlined 表达 default。
- 仅图标按钮必须有 tooltip；陌生功能优先使用“图标 + 短文字”。
- 不在按钮文字中使用 Unicode 箭头代替图标。
- 图片卡片优先保证图像面积；元数据覆盖层要轻，不能用厚边框压缩图像。
- 图片加载失败、NSFW 遮罩和选中态必须有不同的语义表现，不能都依靠半透明黑层。

---

## 11. 组件规范

### 11.1 页面与导航

- 页面头部高度建议 52–56px，包含标题和该页面最高频操作。
- 全局导航与页面内部导航在视觉上区分：全局导航更稳定，局部导航更轻。
- 当前导航项使用淡主色背景和主色前景，默认不加完整描边。
- 侧栏与内容区优先通过不同 surface 区分；必要时只保留一条低对比分隔线。
- 不在页面内部重复全局标题、图标和说明，避免无价值的双层 header。

### 11.2 卡片与分组

- 默认分组方式：标题 + 留白；其次是低对比色面；最后才是边框。
- 普通静态卡片：`surfaceContainerLow`、12px 圆角、无边框、无阴影。
- 普通信息与设置卡片：hover 只提高色面对比；selected 使用主色淡底。图像内容卡片按第 13.3 节使用统一的轻缩放与柔和阴影反馈。
- 卡片内部存在多个小节时，用 16–24px 垂直留白或局部短分隔线，不再给每个小节套卡片。
- 卡片标题栏只在需要共同操作或独立语义时存在；不要每张卡片都复制彩色图标底座。
- 嵌套容器最多两层，第三层必须有明确交互理由。

### 11.3 按钮

优先级从高到低：

1. `FilledButton`：当前区域唯一主操作。
2. `FilledButton.tonal`：重要但非主操作。
3. `TextButton`：取消、返回、低频操作。
4. `IconButton`：高频、语义明确的工具栏动作。
5. `OutlinedButton`：仅在必须表达“可选边界”且色面方案不成立时使用。

规则：

- 相邻按钮不能全部是高强调样式。
- 普通图标按钮无常驻边框；hover 时显示轻色面。
- 危险按钮在确认前通常使用 error 前景或淡 error 色面；最终不可逆确认可使用实心 error。
- loading 替换按钮内部内容时保持按钮宽高不变。
- press 可使用轻微色面加深；缩放最多约 0.98，不能让布局或相邻元素跳动。

### 11.4 输入框

- 默认使用填充式输入面：低对比 Control surface，无常驻描边。
- hover 轻微提高背景对比；focus 显示清晰但细的 primary 焦点环。
- 错误状态使用 error 焦点环与字段下方错误文字，不把整片输入框染成高饱和红色。
- prefix/suffix 图标保持次级视觉权重，获得焦点后可随主色增强。
- 清空、显示密码等按钮默认无边框、点击区足够大。
- 多行提示词输入区可以拥有更明显的独立色面，但不能出现多层内凹白框。

### 11.5 Chip、筛选与 segmented control

- Chip 用于短标签、状态和可移除条件，不用于完整句子。
- 默认 chip 使用淡色面；selected 使用主色淡底，通常不加描边。
- 同一组互斥模式优先使用 segmented control 或 tab，不堆多个独立描边按钮。
- 数量很多时允许横向滚动、折叠或弹出筛选面板，但不能压缩到无法阅读的纯图标。

### 11.6 设置项

- 设置页以任务分组，不以底层 provider、service 或数据模型分组。
- 常规设置项使用一行标题 + 可选说明 + 右侧控件，不必每行加分隔线。
- 连续设置行通过 8–12px 行间距组织；仅在语义小节切换时使用短分隔线或 20–24px 留白。
- Switch 负责即时布尔状态；需要确认或产生昂贵副作用的操作使用按钮或对话框。
- 路径、Token 状态等只读信息使用简洁文本/图标状态，不伪装成可编辑输入框。

### 11.7 对话框与浮层

- 对话框只承载必须打断当前流程的决策；能在原位完成的操作不要弹窗。
- 标题简短，正文先说明结果，再说明原因或风险。
- 操作区右对齐：低强调取消在前，主操作在后。
- 对话框内部避免再套完整描边卡片；使用分区色面或留白。
- 浮层必须有明确锚点，Esc 可关闭，焦点进入后应被正确管理。

### 11.8 加载、空状态与错误

- 首次加载使用骨架或居中进度；局部刷新保留已有内容。
- 空状态由一个图标、一个标题、一句可选说明和最多一个主操作组成。
- 错误信息说明“发生了什么”和“下一步能做什么”，不要只显示异常字符串。
- toast 用于短暂确认，不用于必须阅读或必须操作的信息。
- 成功状态不需要在每个动作后使用绿色大卡片；优先原位更新或短 toast。

---

## 12. 布局与响应式

项目是桌面优先，但所有主要界面必须适应窗口缩放。

### 12.1 推荐断点

断点应按内容是否仍可用决定，不为某台设备写死；以下仅作为统一起点：

- `< 600px`：单列、抽屉或折叠导航。
- `600–840px`：紧凑双区布局、图标导航、减少次要文字。
- `840–1180px`：标准桌面布局。
- `> 1180px`：宽桌面，可增加内容列数或辅助面板，但不盲目拉宽表单。

### 12.2 响应式规则

- 优先保持主要内容可用，再压缩装饰、说明和低频操作。
- 工具栏宽度不足时，按优先级将低频操作收入 overflow menu。
- 不能把仍需识别的操作退化成含义不明的纯图标。
- 表单不因窗口变宽无限拉长；图像网格和画布可以利用全部空间。
- 可拖动分栏必须设置合理最小宽度，不能把任一侧压到不可操作。
- 使用局部 `LayoutBuilder.constraints` 判断当前组件可用宽度，不用 `MediaQuery` 减去猜测的固定侧栏宽度。
- 不使用 `FittedBox` 缩小包含文字或交互控件的工具栏；空间不足时按短标签、横向滚动、`Wrap`、overflow menu 的顺序选择方案。
- 响应式变化不应改变功能归属；控件只换布局，不换语义位置。
- 在线画廊顶栏继续遵守 `AGENTS.md` 中更严格的专用布局约束。

---

## 13. 动效与交互反馈

动效用于解释关系和提供反馈，不用于证明界面“有设计感”。

### 13.1 时间基准

业务组件必须从已挂载的 `AppThemeExtension` 或稳定公共 token 获取时长和曲线，不得继续增加页面私有时长。以下范围是统一默认节奏：

- hover / press：100–140ms。
- 普通状态过渡：160–200ms。
- 面板展开、页面内切换：200–260ms。
- 对话框或大型布局变化：240–320ms。

默认曲线语义：

- 进入与状态变化：平滑 ease-out，当前参考为 `Curves.easeOutCubic`。
- 退出：更短的 ease-in 或 ease-out。
- 位移动效：平滑、可中断，不使用 bounce / elastic 作为功能界面默认曲线。

### 13.2 行为规则

- 普通 hover 反馈以色面和前景变化为主；只有内容预览等明确例外才使用空间变换。
- press 缩放仅用于独立按钮/卡片，范围约 0.98–1.0。
- 列表键盘导航不使用重叠滚动动画，不改变文字字重，不让几何结构抖动。
- 展开/折叠应同时处理进入和退出，避免内容瞬间消失。
- 异步操作必须可见；超过约 300ms 时显示局部 loading。
- 尊重 `MediaQuery.disableAnimations`，关闭非必要位移和缩放。
- 主题可以改变动效节奏的细微气质，但不能在高频操作中引入弹跳、延迟或不可预测路径。

### 13.3 图像卡片悬浮反馈

生成结果、图像历史队列、本地图库和在线画廊都属于高频图像浏览场景。它们需要一致且可见的指针反馈，不能按普通静态卡片完全移除悬浮动画。

- 统一使用 `ImageCardHoverMotion`；默认 hover 目标为 `1.01` 倍缩放，时长与曲线来自 `AppThemeExtension.fastDuration` 和 `standardCurve`。
- 缩放只影响绘制，不改变网格占位、图片比例或相邻卡片布局；进入和退出都必须平滑且可中断。
- hover 同时允许一级柔和阴影和操作按钮显现，但不得叠加亮色完整边框、发光或夸张位移。
- selected、focused、loading 和拖拽准备状态保持各自语义，不能依赖缩放表达；历史列表高速滚动时可以暂时关闭 hover 效果。
- `MediaQuery.disableAnimations` 开启时缩放必须回到 `1.0` 且立即完成，同时保留必要的色面、阴影或操作可见性反馈。
- 该规则只适用于可打开、选择或操作的图像内容卡片，不扩散到设置卡片、统计卡片和普通信息分组。

---

## 14. 可访问性、键盘与本地化

- 普通正文与背景至少满足 WCAG AA 对比度；不要依赖低透明度灰字承载关键信息。
- 不能只用颜色表达 selected、error、success；同时使用图标、文字或位置关系。
- 所有主要操作可通过键盘到达，并具有清晰 focus 状态。
- 自定义按钮、开关和可点击卡片必须提供 `Semantics`、disabled、focus、Enter 与 Space 行为；不能只用裸 `GestureDetector` 模拟控件。
- 仅图标操作必须提供 tooltip 和语义标签。
- 桌面点击目标常规不小于 40×40px，触屏目标不小于 44×44px；紧凑视觉图标可以小，但命中区域不能同步缩小。
- 支持文本缩放后，标题和按钮文字不能被固定高度裁切。
- 中、英、日文案长度不同；横向布局必须允许弹性、截断或合理换行。
- 操作按钮优先使用简短动词，说明文本避免重复标题。
- RTL 当前不是主要目标，但不要用字符串空格或硬编码坐标模拟布局。

---

## 15. 多主题系统的边界

项目允许多种主题，但主题是同一产品的不同气质，不是多套互不兼容的产品。当前运行入口固定为暗色模式，但主题系统仍构建 light theme；公共组件必须保持亮暗配色正确，避免未来开放亮色模式时重新改写业务 UI。

### 15.1 所有主题必须保持一致

- 信息架构、控件语义和操作顺序。
- 间距节奏、密度范围和对齐原则。
- 无边框优先的层级策略。
- default / hover / pressed / selected / focused / disabled / loading 状态完整性。
- 可读性、点击区域、键盘焦点和响应式行为。
- 动效目的与大致时长范围。

### 15.2 主题可以变化

- `ColorScheme` 调色板。
- 标题与正文字体家族，但不能破坏尺寸层级。
- 圆角在约定范围内的小幅变化。
- Overlay 层的轻量环境阴影，以及不覆盖内容的背景装饰图形；不得恢复独立 shadow / texture 主题系统。
- 图表和来源标识的辅助色。

### 15.3 主题不得变化

- 把普通控件全部改成 2px 高对比描边。
- 让所有按钮变成胶囊或所有卡片变成大圆角。
- 为高频操作加入 bounce、elastic、旋转或持续动画。
- 使用纹理覆盖输入内容、图像或小字号正文。
- 改变控件大小到影响布局稳定性或可点击性。
- 用主题色替代 error、warning、success 的语义。

---

## 16. Flutter 实现约定

### 16.1 样式来源优先级

1. 公共组件表达的语义角色与状态。
2. `Theme.of(context).colorScheme` / `textTheme`。
3. 已挂载到 `ThemeData` 的 `AppThemeExtension` 圆角、动效与分隔 token。
4. `DesignTokens` 中稳定的尺寸、间距和时长。
5. 经说明的业务例外，仅限组件独有且有明确原因的值。

禁止顺序倒置：不能先写一套局部 `Color`、`BorderRadius`、`Duration`，再尝试让主题适配它。新增语义 token 时必须先完成主题挂载与消费链路，不能只在预设文件中声明。

### 16.2 公共组件原则

- 公共组件默认应当是低装饰、可组合的，不在内部强制常驻描边。
- 调用方可以提升强调级别，但不能为了去掉公共组件边框而反向覆盖大量样式。
- 新建通用组件前先检查 `lib/presentation/widgets/common/` 和相邻功能模块。
- 只抽象真实重复的模式，不为单个页面创建“万能卡片”。
- 组件 API 表达语义，例如 `selected`、`danger`、`emphasis`，而不是暴露十几个颜色参数。

### 16.3 当前组件选型

在公共组件完成进一步收敛前，新代码按以下顺序选型，避免继续增加并行实现：

| 语义 | 默认选择 | 现阶段约束 |
|---|---|---|
| Button | Material 3 `FilledButton` / `.tonal` / `TextButton` / `IconButton`，或现有 `ThemedButton` 兼容入口 | `ThemedButton` 内部同样使用原生 Material 控件，不引入另一套物理/数字按钮交互 |
| Text input | 复用 `ThemedInput` 或同功能已有输入组件 | focus、error、disabled 由统一语义色面表达；不得再包一层常驻外框 |
| Checkbox / Switch / Slider / Radio | Flutter Material 原生控件或现有 themed 兼容入口 | themed 入口内部复用原生控件，保持键盘、Semantics 与状态行为一致 |
| Card / Panel | 声明 surface 语义的简单容器 | `ElevatedCard` 仅用于确有 elevation 的交互层，普通分组不用它制造阴影 |
| Dialog | 复用已有 themed dialog shell；确认操作使用 `ThemedConfirmDialog` | 不新增仅外观不同的 dialog shell；功能专用内容仍共享遮罩、焦点和操作区规则 |
| Toast | `AppToast` | 不在业务页面另建 overlay 通知；后续统一 live-region、堆叠与关闭命中区 |
| Tooltip | 主题 `Tooltip` / `ShortcutTooltip` | 纯图标和快捷键入口必须提供，正文不能长到替代帮助页 |
| Divider | `Divider` / `VerticalDivider` | 仅用于真实结构边界，颜色从主题获取，不把 divider 当装饰 |

`ThemedContainer` 用于普通语义色面，`ElevatedCard` 只用于确有交互层级的卡片；两者不是可以随意互换的“皮肤”。旧 `ComposerCard` 与 `GlassDialog` 已移除，新功能不得仅因看起来特别而重新引入平行容器体系。

### 16.4 推荐实现示例

普通分组色面：

```dart
Container(
  padding: const EdgeInsets.all(16),
  decoration: BoxDecoration(
    color: Theme.of(context).colorScheme.surfaceContainerLow,
    borderRadius: BorderRadius.circular(12),
  ),
  child: content,
)
```

选中项：

```dart
final colors = Theme.of(context).colorScheme;

AnimatedContainer(
  duration: const Duration(milliseconds: 180),
  decoration: BoxDecoration(
    color: selected
        ? colors.primary.withValues(alpha: 0.12)
        : Colors.transparent,
    borderRadius: BorderRadius.circular(8),
  ),
  child: item,
)
```

只在必要位置使用细分隔线：

```dart
BorderSide(
  color: Theme.of(context)
      .colorScheme
      .outlineVariant
      .withValues(alpha: 0.18),
  width: 1,
)
```

不要默认这样实现普通卡片：

```dart
// 不推荐：普通内容同时使用亮边框、阴影和高对比填充。
BoxDecoration(
  color: colors.surface,
  border: Border.all(color: Colors.white.withValues(alpha: 0.35)),
  boxShadow: heavyShadow,
)
```

### 16.5 代码审查关注点

新增或修改 UI 时检查：

- 是否直接写了 `Colors.white`、`Colors.black` 或固定灰色来表达主题层级？
- 是否新增了 `Border.all`？如果去掉，层级是否仍然成立？
- 是否出现外框套内框？
- 是否绕开 `ThemeData`、`AppThemeExtension` 或已有 token？
- 是否为同类控件引入了新的圆角、间距或动效时长？
- hover、focus、pressed、disabled、loading 是否完整？
- 文本变长、窗口缩窄、暗色主题和非默认主题是否仍可用？
- 动画是否解释了变化，还是只增加噪声？

---

## 17. 页面验收清单

### 视觉

- [ ] 第一眼能识别页面主内容和主操作。
- [ ] 普通区域没有白色线框堆叠。
- [ ] 去掉装饰后，排版和留白仍能表达层级。
- [ ] 同类控件的高度、圆角、图标尺寸和间距一致。
- [ ] 页面最多同时存在三个明显表面层级。
- [ ] 暗色与亮色模式均无刺眼边界或浑浊低对比文字。

### 交互

- [ ] hover、pressed、selected、focused、disabled、loading 状态齐全。
- [ ] 状态变化不引起布局跳动。
- [ ] 键盘可达，Esc、Enter、方向键行为符合控件类型。
- [ ] loading 与错误发生在原位，不让旧内容无故消失。
- [ ] 仅图标按钮具有 tooltip。

### 响应式与内容

- [ ] 代表性页面至少在 420、600、840、1180、1600px 中与改动相关的宽度实际渲染；在线画廊额外遵守其 700、840、1180、1600px 回归要求。
- [ ] 在 text scale 1.0、1.3、1.5 中与改动相关的档位检查，不通过缩小文字抵消用户设置。
- [ ] 中文、英文、日文长文案不会遮挡关键操作。
- [ ] 主要内容不会被装饰、侧栏或固定高度挤压。
- [ ] 空状态、错误状态和超长内容均有明确处理。
- [ ] 无 `RenderFlex overflow`、文字裁切和不可点击的小目标。

### 工程

- [ ] 优先复用现有主题与公共组件。
- [ ] 没有新增无理由的硬编码颜色、圆角、间距和 Duration。
- [ ] UI 改动有对应 widget test 或最小回归验证。
- [ ] 桌面 UI 已通过实际 Debug 窗口截图检查，而不只依赖静态分析。

---

## 18. 现有界面的迁移与持续约束

本轮已按以下顺序完成主题根因、公共组件与主要页面的集中收敛；后续触碰相关界面时继续按同一优先级维护：

1. **公共组件默认样式**：输入框、图标按钮、设置卡片、确认对话框。
2. **高频创作路径**：生成页提示词与参数区、图像操作区、导航。
3. **高密度工具栏**：在线画廊筛选、批量操作和分页。
4. **设置页**：移除卡片常驻描边，以留白和色面重建分组。
5. **统计与信息页**：统一卡片层级、图表色彩和空状态。
6. **主题预设**：删除未接线的阴影、效果、纹理与分隔模块，主题个性只通过真实生效的颜色、字体、形状和克制动效表达。

后续改动必须优先修复造成局部不一致的公共根因，不得在统一系统之外增加第三套临时样式；确需边界的场景必须能对应第 5.1 节列出的交互或结构理由。

---

## 19. 最终取舍准则

遇到没有覆盖的设计问题时，按以下顺序决策：

1. 是否让用户更快理解和完成当前任务？
2. 是否让主要内容比容器更醒目？
3. 是否可以用留白或色面解决，而不是增加边框？
4. 是否复用了项目已有的主题语义和组件？
5. 是否在其他主题、窗口宽度和语言下仍然成立？
6. 是否减少了视觉噪声与未来维护成本？

如果两个方案功能相同，选择**容器更少、边框更少、状态更清楚、动效更克制**的那个。
